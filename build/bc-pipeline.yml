kind: "BuildConfig"
apiVersion: "v1"
metadata:
  name: "jenkins"
spec:
  source:
    git:
      uri: "https://github.com/gestrem/pacman"
  strategy:
    jenkinsPipelineStrategy:
      jenkinsfilePath: 'build/'
